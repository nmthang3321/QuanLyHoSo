<UserControl x:Class="QuanLyHoSo.Views.Export.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Grid Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="XUẤT DỮ LIỆU" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Trích xuất dữ liệu hồ sơ theo nhu cầu" Style="{StaticResource MutedText}" Margin="0,4,0,0" />
                </StackPanel>
                <Border HorizontalAlignment="Right" Style="{StaticResource CardBorder}" Padding="12" Width="390">
                    <TextBlock Text="Dữ liệu được xuất đúng theo bộ lọc đang chọn. Hỗ trợ Excel (.xlsx) và CSV (.csv)." TextWrapping="Wrap" Style="{StaticResource MutedText}" />
                </Border>
            </Grid>

            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="1. CHỌN BỘ LỌC DỮ LIỆU" Style="{StaticResource SectionTitleText}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,14" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="1.2*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Margin="0,0,14,12">
                            <TextBlock Text="Thời gian tiếp nhận" />
                            <ComboBox SelectedIndex="0">
                                <ComboBoxItem Content="Tùy chọn" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,14,12">
                            <TextBlock Text="Trạng thái hồ sơ" />
                            <ComboBox ItemsSource="{Binding Statuses}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,14,12">
                            <TextBlock Text="Loại vụ việc" />
                            <ComboBox ItemsSource="{Binding CaseTypes}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="0,0,14,12">
                            <TextBlock Text="Lĩnh vực" />
                            <ComboBox ItemsSource="{Binding Fields}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Column="4" Margin="0,0,0,12">
                            <TextBlock Text="Từ khóa tìm kiếm" />
                            <TextBox Text="Nhập mã hồ sơ, tên người gửi..." />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,0,14,12">
                            <TextBlock Text="Từ ngày" />
                            <TextBox Text="01/08/2026" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,14,12">
                            <TextBlock Text="Địa bàn" />
                            <ComboBox ItemsSource="{Binding Areas}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,14,12">
                            <TextBlock Text="Xã / Phường" />
                            <ComboBox ItemsSource="{Binding Areas}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,14,12">
                            <TextBlock Text="Người xử lý" />
                            <ComboBox ItemsSource="{Binding Processors}" SelectedIndex="0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="4">
                            <TextBlock Text="Sắp xếp theo" />
                            <ComboBox ItemsSource="{Binding SortOptions}" SelectedIndex="0" />
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,14,0">
                            <TextBlock Text="Đến ngày" />
                            <TextBox Text="25/08/2026" />
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                            <Button Style="{StaticResource SecondaryButton}" Content="Đặt lại" Margin="0,0,12,0" />
                            <Button Style="{StaticResource PrimaryButton}" Content="Xem dữ liệu" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="2. CHỌN ĐỊNH DẠNG XUẤT" Style="{StaticResource SectionTitleText}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,14" />
                        <RadioButton IsChecked="True" Margin="0,0,0,8">
                            <RadioButton.Content>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="26" Height="22" CornerRadius="3" Background="#16864B" Margin="0,0,8,0">
                                        <TextBlock Text="XLS" Foreground="White" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="Excel (.xlsx)" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton.Content>
                        </RadioButton>
                        <TextBlock Text="Phù hợp để xem và phân tích dữ liệu" Style="{StaticResource MutedText}" Margin="22,0,0,14" />
                        <RadioButton Margin="0,0,0,8">
                            <RadioButton.Content>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="26" Height="22" CornerRadius="3" Background="#2C9C8A" Margin="0,0,8,0">
                                        <TextBlock Text="CSV" Foreground="White" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="CSV (.csv)" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton.Content>
                        </RadioButton>
                        <TextBlock Text="Phù hợp để xử lý dữ liệu hàng loạt" Style="{StaticResource MutedText}" Margin="22,0,0,18" />
                        <CheckBox Content="Xuất kèm tiêu đề cột" IsChecked="True" Margin="0,0,0,8" />
                        <CheckBox Content="Định dạng ngày dd/mm/yyyy" IsChecked="True" Margin="0,0,0,8" />
                        <CheckBox Content="Chỉ xuất cột đang hiển thị" Margin="0,0,0,18" />
                        <Border Background="#F0F6FF" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Padding="12">
                            <TextBlock Text="Hệ thống sẽ xuất đầy đủ dữ liệu theo bộ lọc đã chọn." TextWrapping="Wrap" Style="{StaticResource MutedText}" />
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="3. XEM TRƯỚC DỮ LIỆU" Style="{StaticResource SectionTitleText}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,14" />
                        <Border Background="#F0F6FF" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                            <TextBlock Text="Tổng số hồ sơ: 248" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
                        </Border>
                        <DataGrid ItemsSource="{Binding PreviewRecords}" Height="246">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="STT" Binding="{Binding Index}" Width="55" />
                                <DataGridTextColumn Header="Mã hồ sơ" Binding="{Binding RecordCode}" Width="130" />
                                <DataGridTextColumn Header="Ngày tiếp nhận" Binding="{Binding ReceivedDate}" Width="120" />
                                <DataGridTextColumn Header="Người gửi đơn" Binding="{Binding SenderName}" Width="*" />
                                <DataGridTextColumn Header="Địa bàn" Binding="{Binding AreaName}" Width="110" />
                                <DataGridTextColumn Header="Loại vụ việc" Binding="{Binding CaseType}" Width="115" />
                                <DataGridTextColumn Header="Lĩnh vực" Binding="{Binding Field}" Width="150" />
                                <DataGridTemplateColumn Header="Trạng thái" Width="132">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                                    CornerRadius="4"
                                                    Height="22"
                                                    Padding="7,1"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Status}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}, ConverterParameter=Foreground}" />
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Margin="0,16,0,0">
                            <TextBlock Text="Hiển thị 1 - 5 của 248 kết quả" Style="{StaticResource MutedText}" VerticalAlignment="Center" />
                            <Button Style="{StaticResource PrimaryButton}" Content="Xuất dữ liệu" HorizontalAlignment="Right" MinWidth="170" />
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>

